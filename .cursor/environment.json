{
  "name": "energyosint",
  "user": "ubuntu",
  "install": "bash ./.cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": ".venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000",
      "description": "the json api. run the pipeline first so it has a run to serve."
    },
    {
      "name": "deck",
      "command": ".venv/bin/python -m http.server 8080 --directory docs",
      "description": "the slide deck and the georgia page"
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8000
    },
    {
      "name": "deck",
      "port": 8080
    }
  ]
}

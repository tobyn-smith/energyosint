pandas>=2.0
numpy>=1.24
requests>=2.28
pyyaml>=6.0
matplotlib>=3.7

# Optional: enables the choropleth in plots.py. The pipeline falls back to a
# bar chart if it isn't installed, so it's left out of the hard requirements.
# geopandas>=0.13

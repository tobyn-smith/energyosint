__pycache__/
*.pyc
.venv/
venv/

# Regenerable intermediates — rebuilt by `python pipeline.py`
data/raw/
data/interim/

# outputs/ is kept in the repo so the example results and README images show
# up on GitHub. Delete locally and rerun to regenerate.
